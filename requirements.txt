# --- Core utilities ---
python-dotenv
tqdm
numpy

# --- Web app ---
streamlit

# --- Local models / NLP ---
torch
transformers
sentencepiece
accelerate
datasets
sentence-transformers      # BERT embeddings for book retrieval

# --- PDF, EPUB & image processing ---
PyMuPDF                     # import fitz (handles PDF and EPUB)
Pillow
